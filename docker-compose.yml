version: '3.9'

services:
  jprg-app:
    build: .
    container_name: jprg-swing-lab
    ports:
      - "3000:3000"
    volumes:
      - .:/app
    environment:
      - NODE_ENV=development
    restart: unless-stopped
